{% extends 'base.html.twig' %}

{% block title %}Modifier un Animal
{% endblock %}

{% block body %}
	<div class="ui segment">
		<h1>Modifier un Animal</h1>

		{{ include('animal/_form.html.twig', {'button_label': 'Update'}) }}

		<a href="{{ path('app_animal_index') }}">Retour à la liste</a>

	</div>
	{{ include('animal/_delete_form.html.twig') }}

{% endblock %}
