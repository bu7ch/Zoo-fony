{% extends 'base.html.twig' %}

{% block title %}Modifier {{ continent.name }}{% endblock %}

{% block body %}
<div class="ui segment">
    <h1 class="ui header">
        <i class="edit icon"></i>
        <div class="content">Modifier {{ continent.name }}</div>
    </h1>

    {% include 'continent/_form.html.twig' with {'form': form, 'button_label': 'Modifier'} %}

    <a href="{{ path('app_continent_index') }}" class="ui button">
        <i class="arrow left icon"></i> Retour à la liste
    </a>

    {{ include('continent/_delete_form.html.twig') }}
</div>
{% endblock %}
