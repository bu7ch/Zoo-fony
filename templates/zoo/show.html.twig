{% extends 'base.html.twig' %}

{% block title %}Zoo{% endblock %}

{% block body %}
    <h1>Zoo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zoo.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ zoo.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ zoo.address }}</td>
            </tr>
            <tr>
                <th>Capacity</th>
                <td>{{ zoo.capacity }}</td>
            </tr>
            <tr>
                <th>FoundedYear</th>
                <td>{{ zoo.foundedYear }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ zoo.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_zoo_index') }}">back to list</a>

    <a href="{{ path('app_zoo_edit', {'id': zoo.id}) }}">edit</a>

    {{ include('zoo/_delete_form.html.twig') }}
{% endblock %}
